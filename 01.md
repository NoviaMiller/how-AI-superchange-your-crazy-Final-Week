# 一、选择题 答案解析

---

## 1. Which one of the following statements is not correct? （**③**）

**答案：③**

**解析：**

- **① 正确**：DBMS 全称 DataBase Management System（数据库管理系统），如 MySQL、PostgreSQL、Oracle 等。
- **② 正确**：DBA 全称 DataBase Administrator（数据库管理员），负责数据库的安装、配置、备份、恢复等工作。
- **③ 错误**：1970 年 E. F. Codd 发表的论文提出的是**关系数据模型**（Relational Data Model），而非层次数据模型（Hierarchical Data Model）。层次数据模型最早由 IBM 的 IMS 系统实现，代表是树形结构。
- **④ 正确**：数据模型包含三个要素：数据结构（Structure）、数据操作（Operation）、完整性约束（Constraint），其中数据结构是核心组成部分。

**举一反三 — 易混淆点：**
| 数据模型 | 提出者/代表 | 特点 |
|---|---|---|
| 层次模型 | IBM IMS | 树形结构，一对多 |
| 网状模型 | CODASYL | 图结构，多对多 |
| 关系模型 | E. F. Codd (1970) | 二维表，SQL |

**新例题：** 以下关于数据模型的描述，错误的是？
- A. 关系模型用二维表组织数据
- B. 层次模型用图结构组织数据
- C. 网状模型允许一个子结点有多个父结点
- D. E. F. Codd 是关系模型的奠基人

<details>
<summary>答案与解析</summary>

**答案：B**

解析：层次模型用**树形结构**组织数据，一个子结点只能有一个父结点；网状模型才用图结构，允许一个子结点有多个父结点。其余选项均正确。

</details>

---

## 2.如果有两个关系 T1, T2。客户要求每当从 T2 中删除一条记录时，T1 中特定记录就需要被改变，我们需要定义什么来满足该要求。（**②**）

### 前置知识：约束 (Constraint)、触发器 (Trigger)、视图 (View) 三者辨析

> 这三个概念初学者极容易混淆，让我们用一个生活中的类比来建立直觉。

---

**1. 约束 (Constraint) — 门卫大叔**

约束就像是数据库的"门卫"，它站在门口检查每一个想进门的记录，**符合规定才放行，不符合就拒绝**。

常见约束类型：
| 约束类型 | 作用 | 例子 |
|---|---|---|
| PRIMARY KEY | 唯一标识每一行，非空且唯一 | 学号 |
| FOREIGN KEY | 引用另一张表的主键，保证引用完整性 | 选课表中的学号 |
| NOT NULL | 该列不能为空 | 姓名不能为空 |
| UNIQUE | 该列值不能重复 | 身份证号 |
| CHECK | 自定义条件检查 | 性别只能是'男'或'女' |

**关键特征**：约束是**被动**的——只在数据"试图进入/修改/删除"时检查，不符合就拒绝。它不能**主动**去做一件事情。

> 就像门卫只能拦住不让你进，但不能因为你到门口了，就主动去帮你倒杯茶。❌

---

**2. 触发器 (Trigger) — 智能管家**

触发器就像是你家的"智能管家"，你对它说："如果发生 X 事，你就立刻去做 Y 事"。它是**事件驱动**的——当某个事件发生时，自动执行预先定义好的动作。

```sql
-- 触发器的基本结构（伪代码）
CREATE TRIGGER 触发器名
AFTER DELETE ON T2           -- 事件：当 T2 表有记录被删除时
FOR EACH ROW                  -- 粒度：每一行被删除都触发
BEGIN
    UPDATE T1 SET ... ;       -- 动作：去修改 T1 表
END;
```

触发器的三要素（ECA 模型）：
- **E**vent（事件）：INSERT / UPDATE / DELETE
- **C**ondition（条件，可选）：满足什么条件才触发
- **A**ction（动作）：触发了要做什么

**关键特征**：触发器是**主动**的——事件发生，自动响应。不需要应用程序调用。

> 智能管家检测到你出门了（事件），就自动关灯、锁门（动作）。你不需要每次出门都打电话叫它做这些事。

---

**3. 视图 (View) — 窗户**

视图是一扇"玻璃窗"——透过它你能看到屋里的东西，但它本身不占用房间空间。

```sql
CREATE VIEW my_view AS
SELECT sno, sname FROM student WHERE sdept = 'CS';
-- 视图只是一个保存下来的 SELECT 查询定义，不存数据
```

| | 基表 (Table) | 视图 (View) |
|---|---|---|
| 存数据？ | **存**，占磁盘空间 | **不存**，只是查询定义 |
| 物理存在？ | 有 | 无，所以叫"虚拟"视图 |
| 能增删改？ | 能 | 部分能（可更新视图），限制多 |
| 本质 | 仓库 | 仓库的一扇窗户 |

**关键特征**：视图是**封装的查询**，主要用于简化复杂查询或隐藏敏感列。它不会响应事件。

> 窗户让你看到仓库里的货物，但你不会对着窗户喊："窗户！一有人进仓库你就把灯打开！"——窗户做不到，那是触发器的事。

---

**三者一句话总结：**

| | 约束 | 触发器 | 视图 |
|---|---|---|---|
| 像什么 | 门卫 | 智能管家 | 窗户 |
| 核心作用 | 限制数据合法性 | 事件驱动自动执行 | 封装查询 / 安全隔离 |
| 主动 or 被动 | 被动检查 | **主动响应** | 被动（只是查询） |

---

**回到本题**：题目说"每当从 T2 删除记录时，T1 需要被修改"，这是典型的"事件→动作"模式，自然是**触发器**的职责。约束只能拒绝删除，做不到"顺便改 T1"；视图根本不会响应任何事件。

**答案：② — 在 T2 上定义 trigger**

**解析：**

核心需求是"当某个事件发生时，自动执行某个动作"，这正是**触发器（Trigger）**的典型应用场景。

- **① 约束（Constraint）**：只能限制数据的合法性（如 CHECK、FOREIGN KEY），无法主动执行修改操作。
- **② 触发器（Trigger）**：在 T2 上定义 `AFTER DELETE` 触发器，当 T2 删除记录时自动触发对 T1 的 UPDATE。语法大致为：
  ```sql
  CREATE TRIGGER update_t1_after_t2_delete
  AFTER DELETE ON T2
  FOR EACH ROW
  BEGIN
      UPDATE T1 SET ... WHERE ...;
  END;
  ```
- **③④ 视图（View）**：视图是虚拟表，本身不存储数据，无法在删除事件发生时自动响应。

**核心区分：**
| 机制 | 作用 | 能否自动响应事件 |
|---|---|---|
| 约束（Constraint） | 限制数据合法性 | 否（被动检查） |
| 触发器（Trigger） | 事件驱动自动执行 | **是** |
| 视图（View） | 封装查询，简化访问 | 否 |

**新例题：** 某银行规定，当账户余额低于 100 元时，系统应自动向该账户发送提醒。最佳实现方式是？
- A. 在账户表上添加 CHECK 约束
- B. 在账户表上创建 AFTER UPDATE 触发器
- C. 创建账户余额视图
- D. 在应用层用定时任务轮询

<details>
<summary>答案与解析</summary>

**答案：B**

解析：CHECK 约束只能阻止不合法的数据写入，不能"提醒"；视图只是查询封装；定时轮询无法实时响应。触发器能够在数据变更时自动执行逻辑。

</details>

---

## 3. 在数据库系统中，日志文件可以用于以下哪个？（**③**）

**答案：③ — 数据库启动恢复**

**解析：**

日志文件（Log）记录了事务对数据库的所有修改操作（如 Redo Log 和 Undo Log）。当系统崩溃后重启时，DBMS 通过重做（Redo）已提交事务和撤销（Undo）未提交事务来恢复一致性状态。

各选项分析：
| 选项 | 判断 | 说明 |
|---|---|---|
| 并发性 | ✗ | 由锁、时间戳、MVCC 等并发控制机制保证 |
| 安全性 | ✗ | 由权限控制（GRANT/REVOKE）、审计等保证 |
| 启动恢复 | **✓** | 日志记录了所有修改，崩溃后可据此恢复 |
| 检测死锁 | ✗ | 由死锁检测算法（如等待图）完成 |

> **比喻**：日志就像飞机的黑匣子。黑匣子不能防止事故（安全性），不能保证飞机不堵机（并发性），但可以在出事后帮助调查、还原事故原因（恢复）。

**新例题：** 以下哪项是数据库日志文件的核心作用？
- A. 优化查询性能
- B. 实现事务的故障恢复
- C. 管理用户权限
- D. 防止 SQL 注入

<details>
<summary>答案与解析</summary>

**答案：B**

解析：日志文件的核心作用就是故障恢复（transaction recovery），记录事务的所有修改以便在崩溃后重做（Redo）或撤销（Undo）。

</details>

---

## 4. 张三需要在 t1 表进行数据查找，在 t1 表上他最少需要什么权限：（**①**）

**答案：① — SELECT**

**解析：**

- **SELECT**（查询权限）是执行查找操作的最小必需权限。有了 SELECT 权限就可以使用 `SELECT ... FROM t1 ...` 进行数据查找。
- **INSERT、DELETE、UPDATE** 等都是针对写操作的权限，查找不需要这些权限。
- **QUERY** 不是 SQL 标准中的权限关键字。

> **举例**：就像去图书馆查阅书籍（SELECT），你只需要"阅览证"即可；不需要"借书证"（INSERT）或"管理员证"（DELETE）。

**新例题：** 用户王五需要向 t2 表新增一条记录，他最少需要的权限是？
- A. SELECT
- B. INSERT
- C. UPDATE
- D. DELETE

<details>
<summary>答案与解析</summary>

**答案：B**

解析：新增记录对应 `INSERT INTO` 语句，最少需要 INSERT 权限。部分场景若含外键校验，可能还需要关联表的 SELECT 权限，但题目问"最少"，INSERT 即为核心答案。

</details>

---

## 5. 在 DBMS 的关系中（**①**）

**答案：① — 外关键字 (foreign key) 属性值可以为 NULL**

**解析：**

逐一分析：

- **① 正确**：外键可以为 NULL。比如学生表中的 `advisor_id`（导师编号），某些学生可能还没有分配导师，此时该字段可以为 NULL。NULL 表示"不存在引用"。
- **② 错误**：关键字（Key/主键）必须满足实体完整性约束——**主键不能为 NULL**。因为主键是元组的唯一标识，如果为 NULL 就失去了标识意义。
- **③④ 均错误**：并非"任何"属性都能或都不能为 NULL，取决于是否定义了 NOT NULL 约束。

> **记忆口诀**："主键非空且唯一，外键可空引用灵。"

**新例题：** 关于关系数据库的完整性约束，以下说法正确的是？
- A. 主键可以包含空值
- B. 外键的值必须在被引用表的主键中存在，或为空值
- C. 任何属性默认都不能为空
- D. 外键不能为空

<details>
<summary>答案与解析</summary>

**答案：B**

解析：外键的引用完整性约束：要么是被引用表主键中存在的值，要么是 NULL。A 错在主键不能为空；C 错在默认可为空（除非显式 NOT NULL）；D 错在外键可以为 NULL。

</details>

---

### 前置知识：虚拟视图 (View) 完全图解

> 如果你把"虚拟视图"理解成"一个真实的表存在于硬盘上"，那你就完全理解错了。我们从零开始建立概念。

---

**1. 什么是视图？——一句话定义**

视图 = 一个**被命名并保存下来的 SELECT 查询语句**。它不存数据，只存查询逻辑。

```sql
-- 创​建视图
CREATE VIEW cs_student AS
SELECT sno, sname, sbirth
FROM student
WHERE sdept = '计算机系';

-- 查询视图（像查表一样查它）
SELECT * FROM cs_student WHERE sgender = '男';
```

当你执行 `SELECT * FROM cs_student` 时，数据库内部实际执行的是：
```sql
SELECT * FROM (
    SELECT sno, sname, sbirth FROM student WHERE sdept = '计算机系'
) AS cs_student
WHERE sgender = '男';
```

---

**2. 为什么叫"虚拟"视图？——和基表对比**

| | 基表 (Base Table) | 视图 (View) |
|---|---|---|
| 数据存储 | **在磁盘上占有物理空间** | ❌ 不占空间，只是一个查询定义 |
| 独立性 | 独立存在 | 依赖于基表，基表没了视图也没了 |
| 物理 vs 逻辑 | 物理存在 | 逻辑存在（"虚拟"的含义） |
| 修改数据 | 直接 INSERT/UPDATE/DELETE | 有条件限制（见下文） |
| 本质比喻 | 仓库本身 | 仓库的一扇窗户 |

> **核心认知**：视图没有单独的数据存储空间。如果你创建一个包含 100 万行的视图，它不会在磁盘上多占 100 万行的空间。数据仍然在基表里，视图只是"透过窗看"而已。

---

**3. 视图有什么用？**

| 用途 | 说明 |
|---|---|
| **简化查询** | 把复杂的多表 JOIN 封装成视图，以后直接 `SELECT * FROM 视图` |
| **安全隔离** | 给用户视图权限而不给基表权限，隐藏敏感列（如工资） |
| **逻辑独立性** | 基表结构变了，只需修改视图定义，应用层代码不变 |

> **关键答案来自这里**：因为你可以 `SELECT ... FROM 视图名`，所以"在查询中虚拟视图可以当作表来使用"是**绝对正确的**。

---

**4. 视图可更新吗？——重要但有限制**

**不是所有视图都支持 INSERT/UPDATE/DELETE**。只有满足以下条件的"可更新视图"才支持：

✅ 可更新的条件（简化版）：
- 基于**单张**基表（不含 JOIN）
- 不含聚合函数（SUM、AVG、COUNT 等）
- 不含 DISTINCT、GROUP BY、HAVING
- SELECT 子句中不含表达式、不含子查询

❌ 不可更新的例子：
```sql
-- 这个视图不可更新（含聚合函数）
CREATE VIEW avg_score AS
SELECT cno, AVG(score) AS avg_s
FROM sc GROUP BY cno;
-- 对 avg_score 执行 INSERT 没有意义——"平均分"要往哪里插？
```

---

**5. 回到题目——逐选项分析备忘录**

| 选项 | 说法 | 对错 | 原因 |
|---|---|---|---|
| ① | 所有虚拟视图都是可更新的 | ❌ | 含 JOIN / 聚合函数等的视图不可更新 |
| ② | 数据库为虚拟视图在磁盘上单独划分空间 | ❌ | "虚拟"的意思就是不占空间，只存查询定义 |
| ③ | 查询中虚拟视图可以当作表来使用 | ✅ | 可以 `SELECT ... FROM view_name` |
| ④ | 所有可更新视图都允许 INSERT | ❌ | 某些可更新视图只支持 UPDATE/DELETE，不支持 INSERT |

---

## 6. 关于虚拟视图的描述中下面哪个说法是正确的：（**③**）

**答案：③ — 在查询中虚拟视图可以当作表来使用**

**解析：**

- **① 错误**：并非所有视图都可更新。只有满足特定条件的视图（如基于单表、不含聚合函数、不含 DISTINCT 等）才是可更新的。
- **② 错误**：视图是"虚拟表"，**不单独占用磁盘空间存储数据**。视图只是一个保存的 SQL 查询定义，数据来源于基表。这是"虚拟"二字的含义。
- **③ 正确**：在查询中，视图可以像普通表一样被 `SELECT ... FROM view_name` 引用，这是视图的核心用途。
- **④ 错误**：可更新不等同于允许 INSERT。比如某些可更新视图只允许 UPDATE 和 DELETE，但对 INSERT 有限制（如涉及多表连接的视图）。

> **比喻**：视图就像一扇"窗户"——透过它你能看到房间里的物品（基表数据），但窗户本身并不存放物品。

**新例题：** 以下关于视图的说法，错误的是？
- A. 视图可以提高数据安全性，隐藏敏感字段
- B. 所有视图都支持 INSERT 操作
- C. 视图可以简化复杂查询
- D. 视图不存储实际数据

<details>
<summary>答案与解析</summary>

**答案：B**

解析：并非所有视图都支持 INSERT，只有"可更新视图"才支持写操作，且条件较为严格。A、C、D 均为视图的正确特性。

</details>

---

## 7. 数据库元素 A 初始值为 5，T 执行后提交。第 5）和第 6）步描述正确的是？（**③**）

**答案：③ — 5) FLUSH LOG, 6) OUTPUT(A)**

**解析：**

本题考察 **Undo Log 的写规则**。日志记录了 `<T, A, 5>`，这是 Undo 日志格式 `<事务ID, 数据项, 旧值>`。

Undo 日志的 Write-Ahead Logging 规则：
1. 写日志到内存缓冲区 → 2. **FLUSH LOG**（将日志刷到磁盘）→ 3. **OUTPUT**（将数据刷到磁盘）

也就是说：**在将数据写到磁盘之前，必须先确保对应的 Undo 日志已经持久化**。否则，如果在 OUTPUT 后、写日志前崩溃，恢复时就没有 Undo 信息来回滚了。

结合题意：
- 第 4 步后，WRITE(A,t) 已执行（内存中 A 已改为 10），日志已记录 `<T,A,5>`。
- T 已提交（COMMIT），接下来要将日志和数据持久化到磁盘。
- 按 Undo 规则：**先 FLUSH LOG（第 5 步），再 OUTPUT(A)（第 6 步）**。

> **核心原则**：先保证退路（日志落盘），再走下一步（数据落盘）。

**新例题：** 对于 Undo 日志，以下关于写顺序的描述正确的是？
- A. 先 OUTOUT 数据到磁盘，再 FLUSH LOG
- B. 先 FLUSH LOG，再 OUTPUT 数据到磁盘
- C. FLUSH LOG 和 OUTPUT 可以任意顺序执行
- D. Undo 日志不需要 FLUSH LOG

<details>
<summary>答案与解析</summary>

**答案：B**

解析：Undo 日志遵循 WAL（Write-Ahead Logging）原则——日志必须先于数据持久化。这样即使数据写入后崩溃，也能通过日志回滚。

</details>

---

### 前置知识：范式 & 数据冗余——从零开始理解

> 范式（Normal Form）是数据库设计中最让初学者头疼的概念之一。我们从一个具体的"烂表设计"出发，逐步推导出范式的动机和含义。

---

**0. 为什么要学范式？——一个直观的例子**

假设你设计了一张学生选课表，把所有信息塞在一起：

| 学号 | 姓名 | 系别 | 系主任 | 课程号 | 课程名 | 成绩 |
|---|---|---|---|---|---|---|
| 1001 | 张三 | CS | 王教授 | C01 | 数据库 | 90 |
| 1001 | 张三 | CS | 王教授 | C02 | 操作系统 | 85 |
| 1002 | 李四 | CS | 王教授 | C01 | 数据库 | 78 |
| 1003 | 王五 | MATH | 李教授 | C03 | 高数 | 88 |

这张表有什么问题？

- **冗余 (Redundancy)**：张三的姓名、系别、系主任出现了 2 次；王教授的名字重复了 3 遍
- **更新异常 (Update Anomaly)**：王教授升职为院长，需要改 3 条记录——漏改一条就数据不一致了
- **插入异常 (Insert Anomaly)**：新开一门课还没有学生选，这门课就无法录入（因为缺少主键学号）
- **删除异常 (Delete Anomaly)**：王五退课，删掉他唯一的记录后，"高数"这门课的信息也丢了

> 范式理论的终极目标：**通过拆分表结构，消除这些异常，同时保证数据不丢失（无损分解）。**

---

**1. 第一范式 (1NF) — 最基本的要求**

**定义**：属性值必须是**原子**的（不可再分）。

❌ 反例（非 1NF）：
| 学号 | 姓名 | 联系方式 |
|---|---|---|
| 1001 | 张三 | 北京, 上海 |

> "联系方式"不是原子的——里面包含多个城市。

✅ 改正（1NF）：
| 学号 | 姓名 | 联系方式 |
|---|---|---|
| 1001 | 张三 | 北京 |
| 1001 | 张三 | 上海 |

> **要点**：1NF 是现代关系数据库的基本要求，几乎所有表天然满足 1NF。满足 1NF 是成为"关系"的最低门槛。

---

**2. 第二范式 (2NF) — 消除部分函数依赖**

**定义**：在 1NF 基础上，**非主属性必须完全函数依赖于候选码**（不能只依赖候选码的一部分）。

这是最抽象的一句，拆解如下：
- **候选码**：能唯一标识一行的一组属性（可能是多列的组合键）
- **非主属性**：不在任何候选码中的属性
- **部分函数依赖**：组合主键中，某个非主属性只依赖于组合键的**一部分**，而不是全部

❌ 反例（非 2NF）：

回到开头的烂表，假设主键是 `(学号, 课程号)`：
- `姓名` 只依赖于 `学号`（跟课程号无关）——这是**部分依赖**
- `课程名` 只依赖于 `课程号`（跟学号无关）——这也是**部分依赖**

✅ 改正（2NF）：拆分成三张表
- 学生表：`(学号, 姓名, 系别, 系主任)` — 主键：学号
- 课程表：`(课程号, 课程名)` — 主键：课程号
- 选课表：`(学号, 课程号, 成绩)` — 主键：(学号, 课程号)

> **要点**：2NF 针对**组合主键**。如果表只有单列主键，它就自动满足 2NF（因为没有"部分依赖"的空间）。

---

**3. 第三范式 (3NF) — 消除传递函数依赖**

**定义**：在 2NF 基础上，**非主属性不能传递依赖于候选码**。

什么是传递依赖？
- A → B，B → C，那么 A → C 就是**传递依赖**（B 是中间人）。

❌ 反例（非 3NF）：

学生表 `(学号, 姓名, 系别, 系主任)`：
- `学号 → 系别`（通过学号能确定系别）
- `系别 → 系主任`（通过系别能确定系主任）
- 所以 `学号 → 系主任` 是**传递依赖**（系别是中间人）

这就导致冗余：同一个系的每个学生，都要存一遍系主任名字。

✅ 改正（3NF）：再拆分
- 学生表：`(学号, 姓名, 系别)` — 主键：学号
- 系别表：`(系别, 系主任)` — 主键：系别

> **要点**：3NF 要求"非主属性直接依赖于候选码"，不能拐弯。**数据冗余往往就是不满足 3NF 的表现**——但范式的判断标准是函数依赖关系，不是肉眼数重复数据。

---

**4. BC 范式 (BCNF) — 3NF 的加强版**

**定义**：每个**非平凡的函数依赖**中，**决定因素必须包含一个候选码**（即决定因素必须是超键）。

一句话区别：
| | 3NF | BCNF |
|---|---|---|
| 要求 | 传递依赖不能从"非主属性→非主属性" | 任何决定因素都必须是候选码 |
| 例子 | 允许 `A→B, B→C` 中 B 是主属性 | 不允许 |
| 严格程度 | 较宽松 | **更严格** |

✅ 如果满足 BCNF，**一定满足 3NF**。反过来不一定。

> 回忆：**候选码** = 能唯一标识一行的最小属性集合；**超键** = 能唯一标识一行的属性集合（可以包含多余列）；**主属性** = 属于某个候选码的属性。

---

**5. 范式包含关系图**

```
    1NF (原子性)
     ↑
    2NF (无部分依赖)
     ↑
    3NF (无传递依赖，非主→非主)
     ↑
   BCNF (任何决定因素都是候选码)
     ↑
    4NF (无多值依赖)
     ↑
    5NF (无连接依赖)
```

> 越往上越严格，高范式**自动满足**所有低范式的条件。所以 **BCNF ⇒ 3NF ⇒ 2NF ⇒ 1NF**。

---

**6. 数据冗余 (Data Redundancy) 到底是什么？**

**定义**：同样的数据在多个地方重复存储。

- 冗余≠不满足范式（虽然不满足范式往往导致冗余）
- 不满足范式也可能不冗余（运气好，数据量小）
- **范式判断靠的是函数依赖的逻辑关系，不是看有没有重复值**

> 打个比方：一份 Word 文档复制了三份存在三个文件夹，这叫冗余。但即使只存了一份，如果文档本身的逻辑有重复信息（比如第 3 页和第 7 页都有同一个人的电话号码），那也是潜在的"不满足范式"。**范式管的是"逻辑结构"，不是"磁盘占用"。**

---

**7. 回到题目——逐选项分析**

| 选项 | 说法 | 对错 | 原因 |
|---|---|---|---|
| ① | 一个关系属于 1NF，就属于 3NF | ❌ | 1NF 只是原子性，离 3NF 还差 2NF 和消除传递依赖两步 |
| ② | 一个关系属于 BCNF，就属于 3NF | ✅ | BCNF 比 3NF 更严格，满足高范式自动满足低范式 |
| ③ | 有数据冗余就是 3NF | ❌ | 判断 3NF 要看传递依赖，不能以冗余反推；而且有冗余通常说明**不满足**3NF |
| ④ | 没有数据冗余就是 3NF | ❌ | 有没有冗余跟是否满足 3NF 是两回事，"看起来没重复"不等于"消除了传递依赖" |

---

## 8. 关于第三范式描述正确的是（**②**）

**答案：② — 一个关系模式属于 BC 范式，它就属于第三范式**

**解析：**

范式之间的包含关系（从低到高）：
```
1NF ⊃ 2NF ⊃ 3NF ⊃ BCNF ⊃ 4NF ⊃ 5NF
```
- BCNF 比 3NF 更严格，所以满足 BCNF **一定满足** 3NF（② 正确）。
- ① 错误：1NF 只要求属性原子性，远达不到 3NF（3NF 还要消除传递依赖）。
- ③④ 错误：范式的判断依据是**函数依赖**，而非"有没有数据冗余"。有冗余不一定违反 3NF，没冗余也不一定就是 3NF。

> **记忆**：BCNF 是 3NF 的加强版，3NF 允许"主属性对候选码的传递依赖"，BCNF 不允许。

**新例题：** 如果一个关系模式 R 满足 BCNF，那么 R 一定满足？
- A. 仅 1NF
- B. 1NF 和 2NF
- C. 1NF、2NF 和 3NF
- D. 仅 BCNF

<details>
<summary>答案与解析</summary>

**答案：C**

解析：范式是逐级包含的——高范式自动满足所有更低范式。BCNF ⊂ 3NF ⊂ 2NF ⊂ 1NF，所以满足 BCNF 必然满足 1NF、2NF、3NF。

</details>

---

## 9. R(A,B) 有两个元组 (a,b)、(b,c)，FOREIGN KEY (D) REFERENCES R(A)。下面哪个元组不能出现在 S 中？（**②**）

**答案：② — (b, c)**

**解析：**

外键约束：S 的 D 属性值必须在 R 的 A 属性中存在（或被引用属性是主键/唯一键），或为 NULL。

R(A) 中的值集合 = {a, b}。

逐个检查：
| 选项 | S 的元组 | D 的值 | 在 R(A) 中？ | 能否出现 |
|---|---|---|---|---|
| ① | (a, b) | b | a, **b** ✓ | 能 |
| ② | (b, c) | **c** | a, b → ✗ | **不能** |
| ③ | (b, a) | **a** | **a**, b ✓ | 能 |
| ④ | (b, NULL) | NULL | 外键可为 NULL ✓ | 能 |

> **关键**：引用完整性只检查外键值是否在被引用列中"存在"，不检查被引用行的其他列。所以选项①中 D=b，只需确认 b 在 R(A) 中存在即可，与 S 的 C 列值无关。

**新例题：** 学生表 S(sno PK, sname)，选课表 SC(sno FK REFERENCES S(sno), cno)。S 中有 sno={1001, 1002}。以下哪个操作会失败？
- A. INSERT INTO SC VALUES (1001, 'C01')
- B. INSERT INTO SC VALUES (NULL, 'C02')
- C. INSERT INTO SC VALUES (1003, 'C03')
- D. INSERT INTO SC VALUES (1002, NULL)

<details>
<summary>答案与解析</summary>

**答案：C**

解析：sno=1003 在学生表 S 中不存在，违反外键约束。A 中 1001 存在，合法；B 中外键为 NULL 合法；D 中 1002 存在（cno 可以为 NULL，除非有 NOT NULL 约束）。

</details>

---

## 10. 下面关于数据库事务处理描述正确的是：（**④**）

**答案：④ — 原子性和持久性是由数据库的日志保证的**

**解析：**

先回顾 ACID 四大特性：

| 特性 | 含义 | 由什么保证 |
|---|---|---|
| **原子性 (Atomicity)** | 事务要么全做，要么全不做 | **日志（Undo Log）** — 失败时回滚 |
| **一致性 (Consistency)** | 事务将数据库从一个一致状态变为另一个一致状态 | 由其他三个特性 + 约束共同保证 |
| **隔离性 (Isolation)** | 并发事务互不干扰 | **并发控制**（锁、时间戳、MVCC 等） |
| **持久性 (Durability)** | 已提交事务的结果永久保存 | **日志（Redo Log）** — 崩溃后重做 |

- ① 错误：一致性不是仅靠并发调度保证的，原子性和隔离性也共同支撑一致性。
- ② 错误：持久性由日志保证，不是并发调度。
- ③ 错误：隔离性由并发控制保证，不是日志。
- ④ **正确**：原子性靠 Undo Log（回滚未完成事务），持久性靠 Redo Log（重做已提交事务）。

> **记忆口诀**："原子持久靠日志，隔离靠锁控并发。"

**新例题：** 数据库系统中，事务的隔离性（Isolation）主要通过什么机制保证？
- A. 日志文件
- B. 并发控制机制（如锁）
- C. 检查点（Checkpoint）
- D. 触发器

<details>
<summary>答案与解析</summary>

**答案：B**

解析：隔离性通过锁（共享锁/排他锁）、时间戳、MVCC（多版本并发控制）等并发控制机制来保证，确保多个事务并发执行时互不干扰。

</details>

---

> **选择题小结**：本章 10 道选择题覆盖了数据库基础概念（DBMS/DBA/数据模型）、触发器、日志与恢复、SQL 权限、完整性约束（主键/外键/NULL）、视图、Undo 日志写规则、范式理论、引用完整性、ACID 特性等核心考点，属于数据库课程的基石内容。
