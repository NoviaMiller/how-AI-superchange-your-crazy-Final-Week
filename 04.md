# 四、规范化理论（12 分）—— 答案与解析

---

## 13. Chase Test 判断无损分解（6 分）

### 英文题干逐词翻译

> Let a **relation** R (A, B, C, D) be **decomposed** into relations with the following three sets of **attributes**: {A, D}, {B, C, D}, and {A, C}, and the **FD's** A→B, B→C, and CD→A **hold** in R. Use the **chase test** to tell whether the **decomposition** of R is **lossless**.

| 英文术语 | 中文翻译 | 通俗解释 |
|---|---|---|
| **relation** | 关系 | 就是数据库里的一张**二维表**。比如 R(A,B,C,D) 就是一张有 A、B、C、D 四列的表 |
| **attribute** | 属性 | 表中的**列**（字段）。比如 "学号" "姓名" 都是属性 |
| **decompose / decomposition** | 分解 | 把一张大表**拆成多张小表**。比如把 R(A,B,C,D) 拆成 R1(A,D)、R2(B,C,D)、R3(A,C) |
| **FD (Functional Dependency)** | 函数依赖 | 一种约束规则。A→B 的意思是：**只要 A 的值确定了，B 的值就唯一确定**。比如学号→姓名，知道学号就知道是谁 |
| **hold** | 成立 / 满足 | 指某个 FD 在关系中**确实有效** |
| **chase test** | 追踪测试 | 一种**算法**，用来检验分解后的几个小表能否通过自然连接**无损失地还原**成原来的大表 |
| **lossless** | 无损的 | 分解后再连接起来，**不丢数据、不多数据**，和原来一模一样 |

### 解题步骤

**已知条件：**
- 原关系：R(A, B, C, D)
- 分解为三个子关系：R1(A, D)、R2(B, C, D)、R3(A, C)
- 函数依赖集 F = {A→B, B→C, CD→A}

---

#### 第一步：构建初始 tableau（画表格）

为每个子关系创建一行，列出所有属性列（A, B, C, D）：

- 如果该属性**属于**这个子关系，填 **带下标的 a**（如 a₁, a₂, a₃, a₄），称为"可区分的符号"
- 如果该属性**不属于**这个子关系，填 **带双下标的 b**（如 b₁₂），称为"不可区分的符号"

> **为什么要这样区分？** a 代表"该行的这个值是真的"，b 代表"该行的这个值是未知的/临时的"。chase 的过程就是尽量把 b 变成 a —— 如果某一行全部变成 a₁, a₂, a₃, a₄，说明分解是无损的。

| 来源 | A | B | C | D |
|------|---|---|---|---|
| R1(A, D) | **a₁** | b₁₂ | b₁₃ | **a₄** |
| R2(B, C, D) | b₂₁ | **a₂** | **a₃** | **a₄** |
| R3(A, C) | **a₁** | b₃₂ | **a₃** | b₃₄ |

**解读表格：**
- R1 有 A 和 D，所以 A 列写 a₁、D 列写 a₄；B 和 C 列是临时符号
- R2 有 B、C、D，所以这三列写 a₂、a₃、a₄；A 列是临时符号
- R3 有 A 和 C，所以 A 写 a₁、C 写 a₃；B 和 D 列是临时符号

#### 第二步：逐个应用函数依赖（FD）

应用 FD 的规则：如果**两行在左边属性上相等**，就让它们**在右边属性上也相等**（把 b 改成 a，或者统一成同一个符号）。

---

**应用 FD1：A → B**

找 A 列值相同的行 → 第 1 行和第 3 行的 A 都是 **a₁**。

现在 B 列：第 1 行是 b₁₂，第 3 行是 b₃₂。它们不相等！需要统一。

把 b₃₂ 改成 b₁₂（统一成更早出现的下标）：

| 来源 | A | B | C | D |
|------|---|---|---|---|
| R1(A, D) | a₁ | b₁₂ | b₁₃ | a₄ |
| R2(B, C, D) | b₂₁ | a₂ | a₃ | a₄ |
| R3(A, C) | a₁ | **b₁₂** ←改了 | a₃ | b₃₄ |

---

**应用 FD2：B → C**

找 B 列值相同的行 → 第 1 行和第 3 行的 B 都是 **b₁₂**。

现在 C 列：第 1 行是 b₁₃，第 3 行是 **a₃**。b₁₃ 应该向 a₃ 看齐（a 代表真实值，优先级更高）：

把 b₁₃ 改成 a₃：

| 来源 | A | B | C | D |
|------|---|---|---|---|
| R1(A, D) | a₁ | b₁₂ | **a₃** ←改了 | a₄ |
| R2(B, C, D) | b₂₁ | a₂ | a₃ | a₄ |
| R3(A, C) | a₁ | b₁₂ | a₃ | b₃₄ |

---

**应用 FD3：CD → A**

找 C 和 D **同时相同**的行 → 第 1 行和第 2 行：C 都是 a₃、D 都是 a₄。

现在 A 列：第 1 行是 **a₁**，第 2 行是 b₂₁。把 b₂₁ 改成 a₁：

| 来源 | A | B | C | D |
|------|---|---|---|---|
| R1(A, D) | a₁ | b₁₂ | a₃ | a₄ |
| R2(B, C, D) | **a₁** ←改了 | a₂ | a₃ | a₄ |
| R3(A, C) | a₁ | b₁₂ | a₃ | b₃₄ |

---

#### 第三步：判断结果

观察第 2 行：**a₁, a₂, a₃, a₄** —— 全是 "a" 符号！

> **规则**：只要 chase 过程中出现一行**全部由 a 符号组成**（即 a₁, a₂, a₃, a₄），就说明分解是**无损连接（lossless join）**的。

### 答案

**该分解是无损连接分解（lossless join decomposition）。**

---

### 举一反三：简单例题

**题目**：有关系 R(X, Y, Z)，FD = {X→Y, Y→Z}。分解为 R1(X, Y) 和 R2(Y, Z)。用 chase test 判断是否无损。

**初始表格**：

| 来源 | X | Y | Z |
|------|---|---|---|
| R1(X, Y) | a₁ | a₂ | b₁₃ |
| R2(Y, Z) | b₂₁ | a₂ | a₃ |

**应用 X→Y**：X 列两行不同（a₁ vs b₂₁），不触发。

**应用 Y→Z**：Y 列两行相同（都是 a₂），Z 列 b₁₃ → 改成 a₃。

| 来源 | X | Y | Z |
|------|---|---|---|
| R1(X, Y) | a₁ | a₂ | **a₃** |
| R2(Y, Z) | b₂₁ | a₂ | a₃ |

第 1 行变成 [a₁, a₂, a₃]，全部是 a！**无损分解** ✓

> **小结**：chase test 的核心就是"把 b 往 a 变"——只要某一行全变成 a，就是无损的。

---

## 14. BCNF 分解（6 分）

### 英文题干逐词翻译

> **Consider** a relation R (A, B, C, D) with FD's AB→C, C→D, and D→A. **Decompose** R into a set of relations that are all in **BCNF**.

| 英文术语 | 中文翻译 | 通俗解释 |
|---|---|---|
| **consider** | 考虑 / 给定 | 就是"给你这样一个关系" |
| **BCNF (Boyce-Codd Normal Form)** | BC 范式 | 比第三范式（3NF）更严格的范式。要求：每个非平凡函数依赖的**左边必须是超键** |
| **decompose** | 分解 | 把表拆成多个小表，每个小表都满足 BCNF |
| **superkey** | 超键 | 能唯一标识一行数据的属性集合。比如学号是超键，(学号, 姓名) 也是超键（但后者有多余属性） |
| **candidate key** | 候选键 | **最小的**超键。去掉其中任何一个属性就不能唯一标识了 |
| **closure** | 闭包 | 从一个属性集出发，通过 FD 能推导出的**所有属性**的集合。记作 X⁺ |
| **trivial FD** | 平凡函数依赖 | 右边 ⊆ 左边，比如 AB→A，这种"废话"依赖 |
| **non-trivial FD** | 非平凡函数依赖 | 右边 ⊈ 左边，比如 AB→C，有实际约束意义 |

### BCNF 判定标准（一句话）

> 对于关系中每一个**非平凡**的函数依赖 X → Y，**X 必须是超键（superkey）**。违反这个条件就需要分解。

---

### 解题步骤

**已知条件：**
- 关系 R(A, B, C, D)
- FD 集合：F = {AB→C, C→D, D→A}

#### 第一步：找出所有候选键（Candidate Keys）

通过求闭包来找：

**什么是闭包？** X⁺ 表示从 X 出发，反复应用 FD，能推导出的所有属性。如果 X⁺ = 全部属性，那 X 就是超键。

- **试 AB**：AB⁺ = AB → (用 AB→C) → ABC → (用 C→D) → **ABCD**。✓ AB 是超键，且是最小的（因为 A⁺=A, B⁺=B 都不是），所以 AB 是**候选键**。

- **试 BC**：B⁺ = B，所以从 BC 开始。BC⁺ = BC → (用 C→D) → BCD → (用 D→A) → **ABCD**。✓ BC 也是候选键。

- **试 D**：D⁺ = D → (用 D→A) → DA。A⁺=A，卡住了，≠ ABCD。✗

- **试 C**：C⁺ = C → (用 C→D) → CD → (用 D→A) → ACD。≠ ABCD。✗

- **试 BD**：BD⁺ = BD → (用 D→A) → ABD。≠ ABCD。✗

- **试 AC**：AC⁺ = AC → (用 C→D) → ACD → (用 D→A 已有A) → ACD。≠ ABCD。✗

**结论**：候选键是 **AB** 和 **BC**。

#### 第二步：逐个检查每个 FD 是否违反 BCNF

> BCNF 规则：FD 的左边必须是超键（闭包 = 全部属性 ABCD）

| FD | 左边 | 左边闭包 | 是超键？ | 是否违反 BCNF |
|---|---|---|---|---|
| AB → C | AB | ABCD | ✓ 是 | **不违反** |
| C → D | C | CDA ≠ ABCD | ✗ 不是 | **违反** |
| D → A | D | DA ≠ ABCD | ✗ 不是 | **违反** |

#### 第三步：选择违反的 FD 进行分解

选择 **C → D**（常用策略：优先选右边属性少的 FD，分解更简单）：

用 C→D 将 R 一分为二：

- **R1(C, D)**：包含 FD 左边+右边。C→D 在 R1 中，C⁺ = CD，C 是 R1 的超键。✓ BCNF
- **R2(A, B, C)**：包含 FD 左边 + 剩下所有属性。

#### 第四步：检查 R2 是否满足 BCNF

将原 FD 集**投影**到 R2(A, B, C) 上，看哪些 FD 还"活"在 R2 中：

| 原 FD | 在 R2 中吗？ |
|---|---|
| AB→C | ✓ A、B、C 都在 R2 中，成立 |
| C→D | ✗ D 不在 R2 中，失效 |
| D→A | ✗ D 不在 R2 中，失效 |

R2 上只剩 AB→C。检查：AB⁺ = ABC（在 R2 范围内），AB 是超键。✓ **R2 也满足 BCNF**。

#### 第五步：确认无损性

R1 ∩ R2 = {C}，而 C→D（C 是 R1 的键），所以 C → R1，满足无损连接条件。**分解是无损的**。

### 答案

**分解结果为：**
- **R1(C, D)**，满足 BCNF（FD: C→D，C 是键）
- **R2(A, B, C)**，满足 BCNF（FD: AB→C，AB 是键）

> ⚠️ **注意**：FD D→A 在分解中**丢失了**（D 和 A 分在了两个表中）。这是 BCNF 分解的正常现象——BCNF **不保证依赖保持（dependency preservation）**。如果题目要求既 BCNF 又保持依赖，有时是不可能的（需要在 BCNF 和 3NF 之间做权衡）。

---

### 举一反三：简单例题

**题目**：R(A, B, C)，FD = {A→B, B→C}。分解成 BCNF。

**解答**：

1. 找候选键：A⁺ = ABC，所以 **A 是候选键**。
2. 检查 BCNF：
   - A→B：左边 A 是超键 ✓
   - B→C：左边 B⁺ = BC ≠ ABC，**违反 BCNF** ✗
3. 用 B→C 分解：
   - R1(B, C)，B→C，B 是键 ✓ BCNF
   - R2(A, B)，A→B，A 是键 ✓ BCNF

**最终结果：R1(B, C) 和 R2(A, B)**

> **做题口诀**：先求候选键 → 再逐条查 BCNF → 用违反的 FD 分解 → 递归检查子表 → 直到全部满足。

---

## 本讲考点总结

| 考点 | Chase Test | BCNF 分解 |
|---|---|---|
| **干什么的** | 判断分解是不是无损的 | 把表拆成满足 BCNF 的小表 |
| **核心技巧** | 画表格 → 逐条用 FD 把 b 变 a | 求闭包找候选键 → 检查左边是不是超键 |
| **判定标准** | 某一行全变成 a₁ a₂ a₃ a₄ | 每个 FD 的左边都是超键 |
| **常见坑** | 别忽略 CD→A 这种**组合左部**的 FD | BCNF 分解可能**丢失 FD**，这是正常的 |
