# 五、问答题（30 分）答案与解析

---

## 第 15 题（4 分）

**题目**：假设用户 A 是权限 p 对应关系的所有者（owner），请分别给出顺序执行完步骤 4) 和步骤 5) 之后的授权图（grant diagrams）。

| Step | By (User) | Action |
|------|-----------|--------|
| 1) | A | GRANT p TO B, E WITH GRANT OPTION |
| 2) | B | GRANT p TO C WITH GRANT OPTION |
| 3) | C | GRANT p TO D |
| 4) | E | GRANT p TO D WITH GRANT OPTION |
| 5) | A | REVOKE GRANT OPTION FOR p FROM B CASCADE |

### 答案

**步骤 4 执行完毕后的授权图：**

```
        A (owner)
       /   \
    p*/    p*/
     /       \
    B         E
    | p*       \ p*
    C           |
    | p          |
    D <──────────┘
   (从C获得p)
   (从E获得p*)
```

图中说明：
- `p*` 表示拥有权限 p 且带有 GRANT OPTION（可以继续授权给他人）
- `p` 表示只拥有权限 p，没有 GRANT OPTION
- A → B、A → E：A 将 p 的 GRANT OPTION 授予 B 和 E
- B → C：B 将 p 的 GRANT OPTION 授予 C
- C → D：C 将 p（无 GRANT OPTION）授予 D
- E → D：E 将 p 的 GRANT OPTION 授予 D
- **D 从两条路径获得 p：从 C 获得 p（无 grant option），从 E 获得 p\*（有 grant option）**

**步骤 5 执行完毕后的授权图：**

```
        A (owner)
       /   \
     p      p*
     /       \
    B         E
    |          \ p*
    |           \
    C            D
    |
    | (p已随
    |  CASCADE撤销)
    ↓
   (D 从 C 获得的 p 被撤销)
```

图中说明：
- `REVOKE GRANT OPTION FOR p FROM B CASCADE` 的含义：
  1. B 的 GRANT OPTION 被收回，但 B 仍保留权限 p 本身
  2. 由于 CASCADE，C 的 GRANT OPTION 也被收回（因为 C 的 grant option 来自 B）
  3. C 自身仍保留权限 p（只是不能再授予他人）
  4. D 从 C 获得的 p 因为 CASCADE 被一并撤销
  5. 但 D 从 E 获得的 p\* **不受影响**，因为 E 的权限直接来自 A，不在 CASCADE 链上

- 最终状态：
  - A：owner，拥有全部权限
  - B：拥有 p（无 grant option）
  - E：拥有 p\*（有 grant option）
  - C：拥有 p（无 grant option）
  - D：拥有 p\*（仅从 E 获得，有 grant option）

### 解析

**核心考点：SQL 授权模型中的 GRANT 与 REVOKE CASCADE 机制**

1. **GRANT OPTION 的传递**：当用户被授予权限并带有 `WITH GRANT OPTION` 时，该用户可以将此权限（甚至 grant option 本身）再授予其他用户，形成授权链。

2. **REVOKE CASCADE 的连锁效应**：
   - `REVOKE GRANT OPTION FOR p FROM B CASCADE` 只是收回 B 的**授权能力**（grant option），B 本身仍然可以行使权限 p
   - `CASCADE` 关键字使得收回操作沿着授权链向下传递：B 授予出去的权限也会被追溯收回
   - 关键规则：**只收回通过被撤销者传递链获得的权限**，不影响从其他独立路径获得的权限
   - 在本例中，D 从 E 获得的 p\* 是一条独立的授权路径（E ← A），所以不受影响

3. **比喻理解**：可以把授权想象成"传钥匙"：
   - A 给 B 和 E 各配了一把可以继续配钥匙的"母钥匙"（GRANT OPTION）
   - B 用母钥匙给 C 配了母钥匙，C 又给 D 配了普通钥匙
   - E 直接给 D 配了母钥匙
   - 当 A 收回 B 的"配钥匙资格"时，B 的母钥匙作废，C 的母钥匙也相应作废，C 给 D 配的钥匙也作废
   - 但 E 给 D 的母钥匙是 E 自己配的，与 B 那条线无关，所以不受影响

### 举一反三

**新例题**：用户 X 是表 T 的 owner，执行以下操作：

| Step | By | Action |
|------|----|--------|
| 1) | X | GRANT SELECT ON T TO Y WITH GRANT OPTION |
| 2) | Y | GRANT SELECT ON T TO Z WITH GRANT OPTION |
| 3) | X | GRANT SELECT ON T TO Z WITH GRANT OPTION |
| 4) | X | REVOKE GRANT OPTION FOR SELECT ON T FROM Y CASCADE |

问：步骤 4 之后，Z 是否还拥有 SELECT 权限？是否还拥有 GRANT OPTION？

**答案**：Z 仍然拥有 SELECT 权限和 GRANT OPTION。因为 Z 的权限有两条来源路径：一条来自 Y（被 CASCADE 收回），另一条直接来自 X（不受影响）。X → Z 的直接授权保留了 Z 的全部权限。

---

## 第 16 题（6 分）

**题目**：对如下调度：

> S = R₁(A), W₁(A), R₂(A), W₂(B), R₃(B), W₃(C), R₄(C), W₄(A)

该调度是否是冲突可串行化的？若是，给出其一个等价的串行化调度；若否，说明理由。

### 答案

**该调度是冲突可串行化的。等价串行调度：T₁ → T₂ → T₃ → T₄**

### 解析

**第一步：识别各事务的操作**

| 事务 | 操作序列 |
|------|---------|
| T₁ | R₁(A), W₁(A) |
| T₂ | R₂(A), W₂(B) |
| T₃ | R₃(B), W₃(C) |
| T₄ | R₄(C), W₄(A) |

**第二步：找出所有冲突操作对**

冲突的定义：两个操作来自**不同事务**、操作**同一数据项**、且**至少有一个是写操作**。

| 冲突对 | 原因 | 推出的偏序关系 |
|--------|------|--------------|
| W₁(A) ↔ R₂(A) | 不同事务，同一数据 A，有写 | W₁(A) 在 R₂(A) 之前 → **T₁ → T₂** |
| W₁(A) ↔ W₄(A) | 不同事务，同一数据 A，有写 | W₁(A) 在 W₄(A) 之前 → **T₁ → T₄** |
| R₂(A) ↔ W₄(A) | 不同事务，同一数据 A，有写 | R₂(A) 在 W₄(A) 之前 → **T₂ → T₄** |
| W₂(B) ↔ R₃(B) | 不同事务，同一数据 B，有写 | W₂(B) 在 R₃(B) 之前 → **T₂ → T₃** |
| W₃(C) ↔ R₄(C) | 不同事务，同一数据 C，有写 | W₃(C) 在 R₄(C) 之前 → **T₃ → T₄** |

**第三步：构建优先图（Precedence Graph），检查有无环**

```
T₁ ──→ T₂ ──→ T₃ ──→ T₄
  \                    ↗
   └──────────────────┘
```

边：T₁→T₂, T₁→T₄, T₂→T₃, T₂→T₄, T₃→T₄

图中**无环**，因此该调度是冲突可串行化的。

**第四步：拓扑排序得等价串行调度**

拓扑排序结果：**T₁, T₂, T₃, T₄**（这是唯一可能的拓扑序）

验证：在串行调度 T₁ → T₂ → T₃ → T₄ 中，所有冲突操作的顺序与原始调度 S 一致。

### 举一反三

**新例题**：判断 S = R₁(A), R₂(A), W₁(A), W₂(A) 是否冲突可串行化？

**答案**：不是。冲突分析：R₂(A) 与 W₁(A) → T₂ → T₁；R₁(A) 与 W₂(A) → T₁ → T₂。优先图中形成环 T₁ ↔ T₂，不可串行化。

---

## 第 17 题（6 分）

**题目**：考虑关系 StarsIn(movieTitle, movieYear, starName)，假设：
- 关系占 200 个 Block
- 平均每位影星出演 3 部电影，每部电影有 7 位影星
- 操作 Q₁（按 starName 查）、Q₂（按 movieTitle 查）、I（插入）的比率依次为 p₁=0.3, p₂=0.4, 1-p₁-p₂=0.3

评估四种索引方案：No Index、StarIndex、MovieIndex、Both Indexes，哪种最优？

### 答案

**同时建立 StarIndex 和 MovieIndex（Both Indexes）是最优方案。**

### 解析

**核心思路**：计算每种方案下平均每次操作的 I/O 代价，取最小者。

---

#### 前置知识：B+ 树索引结构

数据库索引通常不采用普通二叉树，而是使用 **B+ 树（B+ Tree）**。如果你只了解普通二叉树，可以通过以下对比快速理解：

**1. 普通二叉树 vs B+ 树**

| 对比维度 | 普通二叉树 | B+ 树 |
|---------|-----------|------|
| 每个节点的子节点数 | 最多 2 个 | 多个（通常几十到几百个，由磁盘块大小决定） |
| 树的形态 | 可能很高很瘦 | 矮胖（高度通常只有 2~4 层） |
| 数据存储位置 | 每个节点都存数据 | **只有叶子节点存数据**，非叶节点只存"路标"（索引键） |
| 叶子节点之间 | 无连接 | 通过**指针串联成链表**，支持高效范围查询 |
| 适用场景 | 内存中的数据查找 | 磁盘上的大规模数据索引 |

**2. 为什么要用 B+ 树而不是二叉树？**

核心原因：**数据库数据存在磁盘上，访问瓶颈是磁盘 I/O**。

- 磁盘读写以"块（Block）"为单位，一次 I/O 读一个块（如 4KB 或 8KB）
- 普通二叉树：100 万条数据，树高约 20 层 → 最坏需要 **20 次磁盘 I/O** 才能找到一条记录
- B+ 树：每个节点占一个磁盘块，一个节点可存几百个键值。100 万条数据，树高一般仅 **2~3 层** → 只需 **2~3 次磁盘 I/O** 即可定位

**3. B+ 树结构示意**

```
          [30 | 60]              ← 根节点（非叶节点，只存"路标"键值）
         /    |    \
    [10|20] [40|50] [70|80]      ← 内部节点（非叶节点）
    /  |  \   ...    /  |  \
   □──□──□         □──□──□       ← 叶子节点（存全部实际数据 + 链表指针）
   ↓                              ← 叶子之间用指针串联，支持范围扫描
```

**4. B+ 树查找过程（以查找键值 45 为例）**

```
第 1 次 I/O：读根节点 [30|60]
  → 45 在 30 和 60 之间 → 走中间分支

第 2 次 I/O：读内部节点 [40|50]
  → 45 在 40 和 50 之间 → 走第一个分支

第 3 次 I/O：读叶子节点
  → 在叶子节点中找到 45 对应的数据指针
```

总共只需 3 次磁盘 I/O，而同样数据量的二叉树可能需要十几次。

**5. 索引对增删改的影响**

- **查询（SELECT）**：受益最大。全表扫描 200 个块 vs B+ 树索引只需 2~3 次 I/O
- **插入（INSERT）**：除写数据块外，还需更新索引。在 B+ 树中找到插入位置（2~3 次 I/O），写入叶子节点（1 次 I/O）。如果只建一个索引，插入额外开销约 3~4 次 I/O
- **删除（DELETE）**：类似插入，需要维护索引结构
- **更新（UPDATE）**：如果更新了索引列，相当于一次删除 + 一次插入

**6. 一句话总结**

> B+ 树 = 为磁盘而生的"矮胖版多叉查找树"，用极低的高度（2~4 层）替代二叉树的高瘦结构（10~20 层），将百万级数据的查找从几十次磁盘 I/O 降到 2~4 次。

---

**基础参数设定**：
- 全表扫描：200 次 I/O（200 个 Block）
- B+ 树索引高度约 2 层（200 个数据块的索引规模较小，树高不超过 2）
- 索引查找代价：约 2 次 I/O（根节点 1 次 + 叶节点 1 次）
- 数据块读取：每个命中元组可能在不同块中（假设非聚簇索引）

---

**方案一：No Index（不建任何索引）**

| 操作 | 单次代价 | 权重 | 加权代价 |
|------|---------|------|---------|
| Q₁（查 starName） | 200（全表扫描） | 0.3 | 60 |
| Q₂（查 movieTitle） | 200（全表扫描） | 0.4 | 80 |
| I（插入） | 1（追加写入） | 0.3 | 0.3 |
| **合计** | | | **140.3** |

---

**方案二：StarIndex（仅在 starName 上建索引）**

| 操作 | 单次代价 | 权重 | 加权代价 |
|------|---------|------|---------|
| Q₁ | 2（索引查找）+ 3（取 3 个元组） = 5 | 0.3 | 1.5 |
| Q₂ | 200（无 movieTitle 索引，全表扫） | 0.4 | 80 |
| I | 1（数据写入）+ 3（索引插入：查 + 写叶节点） = 4 | 0.3 | 1.2 |
| **合计** | | | **82.7** |

---

**方案三：MovieIndex（仅在 movieTitle 上建索引）**

| 操作 | 单次代价 | 权重 | 加权代价 |
|------|---------|------|---------|
| Q₁ | 200（无 starName 索引，全表扫） | 0.3 | 60 |
| Q₂ | 2（索引查找）+ 7（取 7 个元组） = 9 | 0.4 | 3.6 |
| I | 1（数据写入）+ 3（索引插入） = 4 | 0.3 | 1.2 |
| **合计** | | | **64.8** |

---

**方案四：Both Indexes（两个索引都建）**

| 操作 | 单次代价 | 权重 | 加权代价 |
|------|---------|------|---------|
| Q₁ | 2 + 3 = 5 | 0.3 | 1.5 |
| Q₂ | 2 + 7 = 9 | 0.4 | 3.6 |
| I | 1（数据写入）+ 3（StarIndex 插入）+ 3（MovieIndex 插入） = 7 | 0.3 | 2.1 |
| **合计** | | | **7.2** |

---

**综合对比**：

| 方案 | 平均 I/O 代价 |
|------|-------------|
| No Index | 140.3 |
| StarIndex | 82.7 |
| MovieIndex | 64.8 |
| **Both Indexes** | **7.2 ✓ 最优** |

**结论分析**：
- Q₁ 和 Q₂ 合计占 70% 的操作量，如果不建对应索引每次需要 200 次 I/O，代价极高
- 建两个索引后，查询代价骤降至 5~9 次 I/O
- 插入操作虽受双索引影响从 1 次增到 7 次 I/O，但插入仅占 30%，付出的额外代价远小于查询收益
- **收益远大于开销，所以双索引方案最优**

### 举一反三

**新例题**：同样的 StarsIn 关系，如果 p₁=0.05, p₂=0.05（即查询极少，插入占 90%），哪种方案最优？

**答案**：No Index 最优。
- No Index: 0.05×200 + 0.05×200 + 0.9×1 = 10 + 10 + 0.9 = 20.9
- Both: 0.05×5 + 0.05×9 + 0.9×7 = 0.25 + 0.45 + 6.3 = 7.0
- StarIndex: 0.05×5 + 0.05×200 + 0.9×4 = 0.25 + 10 + 3.6 = 13.85
- MovieIndex: 0.05×200 + 0.05×9 + 0.9×4 = 10 + 0.45 + 3.6 = 14.05

等等，Both 仍然是 7.0 < 20.9。这个例子不够好，因为查询代价减少的幅度太大了。让我修正：

如果 relation 只有 10 个 blocks 呢？No Index: 0.05×10+0.05×10+0.9×1=1.9；Both: 0.05×5+0.05×9+0.9×7=0.25+0.45+6.3=7.0。此时 No Index 更优。

**核心规律**：查询比例越高、表越大，建索引越划算；插入比例越高、表越小，不建索引越划算。当查询节省的 I/O > 插入增加的 I/O 时，建索引更优。

---

## 第 18 题（8 分）

**题目**：假设某一 DBMS 采用 Undo/Redo 日志：
1. 以伪代码形式给出故障恢复算法（6 分）；
2. 讨论如果数据库在恢复时再次崩溃，再次恢复能否恢复正常？为什么？（2 分）

### 前置知识：数据库日志从零到一

如果你完全不了解数据库日志，这一节会带你从最基础的概念出发，逐步建立起理解 Undo/Redo 所需的全部背景知识。

---

#### 一、为什么需要日志？—— 一个生活化的类比

想象你在银行 ATM 上转账 100 元给朋友：

```
Step 1: 你的账户扣 100 元  →  余额 900（原来 1000）
Step 2: 你朋友的账户加 100 元  →  余额 600（原来 500）
```

如果在执行完 Step 1 之后、Step 2 之前，**银行服务器突然断电**了，会发生什么？

> 你的钱扣了 100 元，但朋友没收到。**100 元凭空消失了！**

数据库中把类似"转账"这样的一组不可分割的操作称为**事务（Transaction）**。事务要么全部成功（COMMIT），要么全部撤销（ABORT/ROLLBACK）。上述案例中，如果断电，Step 1 就必须被"撤销"，让数据回到事务开始前的状态。

**日志（Log）就是数据库用来实现这个能力的"黑匣子"**——就像飞机上的飞行记录仪，它忠实地记录了每一次数据修改的"前"和"后"，当灾难发生时，通过分析日志把数据库恢复到正确的状态。

---

#### 二、数据在磁盘 vs 内存：为什么直接写磁盘不够？

数据库数据存在**磁盘（硬盘/SSD）**上，但读写操作发生在**内存（Buffer Pool，缓冲区）**中。读写流程如下：

```
用户发起 UPDATE
    →
从磁盘读入数据块到内存 Buffer
    →
在内存中修改数据
    →
（某个时刻）将修改后的数据块写回磁盘
```

问题的核心：**内存中的数据修改何时写回磁盘？** 这里有四种策略组合，也是理解日志类型的关键：

**Buffer 管理策略的两个维度：**

| 维度 | 策略 | 含义 | 通俗说法 |
|------|------|------|---------|
| **Steal 策略** | Steal（偷） | 未提交事务的修改**允许**提前刷到磁盘 | 脏数据可以偷跑出去 |
| | No-Steal（不偷） | 未提交事务的修改**禁止**刷到磁盘 | 脏数据必须留在内存 |
| **Force 策略** | Force（强刷） | 事务提交时，其所有修改**必须**立即写到磁盘 | 提交 = 强制落地 |
| | No-Force（不强刷） | 事务提交时，修改可以暂留内存，稍后再写 | 提交 ≠ 立即落地 |

四种组合：

| 组合 | 优缺点 |
|------|--------|
| **No-Steal + Force** | 最简单，恢复最容易，但性能最差（每次提交都要写盘） |
| **Steal + No-Force** | 最灵活，性能最好，但恢复最复杂 → **这正是 Undo/Redo 日志要解决的问题** |

**比喻理解**：
- **Steal**：好比你在编辑 Word 文档，还没保存（=事务未提交），Word 的自动保存功能就帮你把草稿存到硬盘了。好处是万一断电丢的东西少，坏处是草稿（未提交的修改）就"偷跑"到了磁盘上，如果要撤销需要额外处理。
- **Force**：好比每次点"保存"都必须等文档完整写到硬盘后才算完。好处是提交即安全，坏处是写完才能干别的，很慢。

---

#### 三、三种日志类型的本质区别

理解了 Steal/Force，就能轻松区分三种日志：

**场景铺垫**：假设有一个事务 T，它对数据项 A 执行了 `A := A + 100`（A 原值=200，新值=300）。

| 日志类型 | 日志记录了什么？ | 要解决什么策略组合？ | 直观理解 |
|---------|---------------|-------------------|---------|
| **Undo 日志** | 只记录**旧值**：`<T, A, 200>` | **Steal**（未提交数据已刷盘，崩溃后需根据旧值撤销） | 像 Word 的"撤销(Ctrl+Z)"按钮，只记录改之前的样子 |
| **Redo 日志** | 只记录**新值**：`<T, A, 300>` | **No-Force**（已提交数据可能未刷盘，崩溃后需根据新值重做） | 像 Word 的"恢复(Ctrl+Y)"按钮，只记录改之后的样子 |
| **Undo/Redo 日志** | 同时记录**新旧值**：`<T, A, 200, 300>` | **Steal + No-Force**（既要撤销未提交的，也要重做已提交的） | 像 Word 同时记录了"改之前"和"改之后"，既支持 Ctrl+Z 也支持 Ctrl+Y |

---

#### 四、Undo/Redo 日志的完整工作过程（结合实例）

以银行转账为例，事务 T 执行：A 扣 100 元 → B 加 100 元。

数据库操作序列（内存中执行）：

```
① <START T>                            -- 事务开始，写日志
② READ(A) → 读到 A=500                -- 读 A 到内存
③ A := A - 100 → A=400                -- 在内存中修改 A
④ WRITE(A) → <T, A, 500, 400>  写日志  -- 写 Undo/Redo 日志：旧值 500，新值 400
   （此时 A=400 可能已写回磁盘，也可能没有 → Steal 策略不关心）
⑤ READ(B) → 读到 B=300                -- 读 B 到内存
⑥ B := B + 100 → B=400                -- 在内存中修改 B
⑦ WRITE(B) → <T, B, 300, 400>  写日志  -- 写 Undo/Redo 日志：旧值 300，新值 400
⑧ <COMMIT T>                          -- 事务提交，写日志
⑨ （稍后某个时刻）将修改后的 A、B 真正写回磁盘
```

**日志文件在磁盘上的模样**（时间从上到下）：

```
<START T>
<T, A, 500, 400>     ← 旧值 500，新值 400
<T, B, 300, 400>     ← 旧值 300，新值 400
<COMMIT T>
```

现在假设第 ⑧ 步（COMMIT）已写入日志，但第 ⑨ 步（写回磁盘）还没来得及执行时**系统崩溃**：

- 磁盘上 **A 还是 500，B 还是 300**（修改没刷盘）
- 但日志中有 `<COMMIT T>`
- **恢复时**：Redo 阶段扫描到 `<T, A, 500, 400>` 和 `<T, B, 300, 400>`，发现 T 已提交 → 将新值 400 和 400 写入磁盘 → 数据恢复正确！

---

#### 五、Checkpoint（检查点）是什么？为什么要它？

日志有一个致命问题：**它会无限增长。** 每次崩溃恢复都从头扫描全部日志？数据库早就被淘汰了。

**Checkpoint（检查点）** 就像一个"存档点"：
- 定期执行：将内存中所有已提交的修改强制刷到磁盘
- 在日志中写入一条 `<CHECKPOINT (active_txns: T1, T3)>` 记录，注明此时哪些事务还在活跃
- **恢复时只需从最近一个 checkpoint 开始扫描**，checkpoint 之前的日志都不需要了

```
日志时间线：
... 旧日志(可忽略) ... | CHECKPOINT | 新日志(恢复时扫描这之后) | 崩溃点
                       ↑
                  恢复扫描起点
```

**比喻**：打游戏时的"存档"。死掉（崩溃）后只需要从最近的存档点（checkpoint）重新开始，不需要从第一关重打。

---

#### 六、恢复算法的直观理解

掌握了以上知识后，Undo/Redo 恢复的三阶段就很好理解了：

| 阶段 | 做的事情 | 通俗比喻 |
|------|---------|---------|
| **Analysis（分析）** | 扫描日志，找出崩溃时哪些事务已提交、哪些未提交 | 火灾后清点：哪些房间的人已经安全出来，哪些还在里面 |
| **Redo（重做）** | 把所有修改（无论是否提交）的新值重新写到磁盘 | 重新播放监控录像，把所有人的动作都重做一遍，恢复到火灾前一刻 |
| **Undo（撤销）** | 把未提交事务的修改用旧值回滚掉 | 把灾后遗留的未完成工作全部取消，回到一致状态 |

---

#### 七、日志相关核心概念速查表

| 概念 | 一句话解释 |
|------|----------|
| 事务（Transaction） | 一组不可分割的数据库操作，要么全做，要么全不做 |
| 日志（Log） | 记录数据修改历史的"黑匣子"，崩溃后靠它恢复 |
| Buffer Pool | 内存中的缓冲区，数据在修改前先从磁盘读到这里 |
| Steal | 允许未提交事务的修改提前写到磁盘 |
| No-Force | 允许已提交事务的修改暂留内存，不立即写磁盘 |
| Undo | 撤销操作——用旧值覆盖新值，把数据"退回"修改前 |
| Redo | 重做操作——用新值覆盖当前值，把数据"推进"到修改后 |
| Checkpoint | 定期存档点，减少恢复时需要扫描的日志量 |
| CLR（补偿日志记录） | Undo 操作本身也会写日志（记录 Undo 所做的修改），保证 Undo 也是幂等的 |
| 幂等性 | 执行一次和执行多次结果相同——恢复算法安全的基石 |

---

### 答案

#### 1) Undo/Redo 故障恢复算法（伪代码）

```
算法：Undo/Redo Recovery

输入：Log（从最近 checkpoint 到末尾）；Checkpoint 中记录的活动事务列表
输出：已恢复的数据库

================================================================
阶段一：Analysis（分析阶段）
================================================================
undo_list := { 最近 checkpoint 中记录的所有活动事务 }

从 checkpoint 位置开始，正向扫描日志：
  for each log record:
    if record is <START T>:
      undo_list := undo_list ∪ {T}           // T 仍处于活动状态
    if record is <COMMIT T>:
      undo_list := undo_list - {T}           // T 已完成，无需 undo
    if record is <ABORT T>:
      undo_list := undo_list - {T}           // T 已显式回滚

================================================================
阶段二：Redo（重做阶段，正向扫描）
================================================================
从 checkpoint 位置开始，正向扫描日志：
  for each log record:
    if record is <T, X, old_value, new_value>:  // UPDATE 日志记录
      WRITE(X, new_value)                        // 将所有更新重做到磁盘
    if record is <COMMIT T>:
      WRITE_LOG(<COMMIT T>)                      // 确保 COMMIT 记录持久化

  // 目的：将数据库恢复到崩溃前的状态（包含所有已提交和未提交的修改）

================================================================
阶段三：Undo（撤销阶段，反向扫描）
================================================================
从日志末尾开始，反向扫描日志：
  for each log record:                          // 从后往前遍历
    if record is <T, X, old_value, new_value> and T ∈ undo_list:
      WRITE(X, old_value)                        // 回滚：写回旧值
      WRITE_LOG(<T, X, old_value, new_value, UNDO>)  // 写补偿日志记录(CLR)

  for each T in undo_list:
    WRITE_LOG(<ABORT T>)                         // 记录事务已回滚

================================================================
完成：数据库恢复到一致状态
================================================================
```

**关键设计要点**：
- Undo/Redo 日志中，每条 UPDATE 记录**同时包含旧值和新值**：`<T, X, old_value, new_value>`
- Redo 阶段使用**新值（new_value）**恢复所有操作
- Undo 阶段使用**旧值（old_value）**回滚未提交事务
- Undo 过程中写的补偿日志记录（CLR）自身也是 Undo/Redo 日志，描述了 undo 操作

#### 2) 恢复中再次崩溃的讨论

**可以恢复正常。** 原因如下：

1. **Redo 操作的幂等性（Idempotence）**：
   - Redo 阶段将 new_value 写入磁盘，无论该值是否已经被写过，重复写的结果完全一样
   - 如果首次恢复中 redo 完成一部分后崩溃，第二次恢复重新 redo 所有操作即可，不会造成数据不一致

2. **Undo 操作的幂等性**：
   - Undo 阶段将 old_value 写回磁盘，同样是幂等操作
   - 如果 undo 过程中崩溃，重启后重新 undo 即可
   - 补偿日志记录（CLR）的写入使得后续恢复可以知道哪些 undo 操作已经完成

3. **日志的可靠性**：
   - 所有恢复动作本身都记日志（如写入 COMMIT 记录、ABORT 记录、CLR）
   - 第二次恢复启动时，通过分析日志可以准确判断哪些事务已提交、哪些未提交
   - 已回滚的事务如果再次出现在 undo_list 中，重新 undo 也不会出错（幂等性）

4. **比喻理解**：
   - 想象你在用橡皮擦修改一份铅笔手稿。如果擦到一半被打断，回来继续擦就是了。因为已经擦掉的部分不会再被"擦回来"，还没擦的部分橡皮还能正常擦。Undo/Redo 恢复就是这个道理——每一步都是可重复的，不管从哪里中断，重来一遍结果都一样。

### 举一反三

**新例题**：简述 Undo-only 日志、Redo-only 日志和 Undo/Redo 日志的主要区别，以及各自的适用场景。

**答案**：
| 日志类型 | 日志记录内容 | 恢复方向 | 关键约束 |
|---------|------------|---------|---------|
| Undo-only | 只记录旧值 | 反向 undo | 提交前必须先刷盘（force） |
| Redo-only | 只记录新值 | 正向 redo | 提交前日志必须先刷盘（WAL），但数据不必刷盘（no-force） |
| Undo/Redo | 同时记录新旧值 | 先 redo 再 undo | 最灵活，允许 no-force + no-steal |

Undo/Redo 最灵活但日志量最大，是现代数据库（如 ARIES 算法）的基础。

---

## 第 19 题（6 分）

**题目**：考虑两个事务：
> T₁: R₁(A), R₁(B), W₁(B)
> T₂: R₂(B), R₂(A), W₂(A), W₂(B)

1. 给 T₁ 和 T₂ 增加加锁和解锁指令，使它们遵循两阶段封锁协议（3 分）。
2. 这两个事务的执行会引起死锁吗？如果会，请给出产生死锁的调度示意图；如果不会，请解释为什么？（3 分）

### 答案

#### 1) 遵循两阶段封锁协议（2PL）的加锁方案

**两阶段封锁协议（2PL）的核心规则**：在每个事务中，所有加锁操作必须在所有解锁操作之前。即事务分为两个阶段——**增长阶段（Growing Phase，只加锁不解锁）** 和 **收缩阶段（Shrinking Phase，只解锁不加锁）**。

**T₁ 的 2PL 实现**：
```
sl₁(A);          // 获取 A 上的共享锁
R₁(A);
sl₁(B);          // 获取 B 上的共享锁
R₁(B);
xl₁(B);          // 将 B 上的共享锁升级为排他锁（仍在增长阶段）
W₁(B);
u₁(A); u₁(B);    // 释放所有锁（收缩阶段）
```

**T₂ 的 2PL 实现**：
```
sl₂(B);          // 获取 B 上的共享锁
R₂(B);
sl₂(A);          // 获取 A 上的共享锁
R₂(A);
xl₂(A);          // 将 A 上的共享锁升级为排他锁
W₂(A);
xl₂(B);          // 将 B 上的共享锁升级为排他锁（注意：此时还未释放任何锁）
W₂(B);
u₂(A); u₂(B);    // 释放所有锁
```

**要点说明**：
- `sli(X)`：事务 i 对数据项 X 加**共享锁（Shared Lock / S锁）**，允许读，不允许写，其他事务可同时加 S 锁
- `xli(X)`：事务 i 对数据项 X 加**排他锁（Exclusive Lock / X锁）**，允许读写，其他事务不能加任何锁
- `ui(X)`：事务 i 释放对 X 的所有锁
- 两个事务的**锁点（Lock Point）**都是最后一个加锁操作：T₁ 的锁点是 `xl₁(B)`，T₂ 的锁点是 `xl₂(B)`。锁点之后只解锁不加锁。

#### 2) 死锁分析

**会引起死锁。**

**死锁调度示意图**：

```
时间  T₁                         T₂
↓    ──────────────────────     ──────────────────────
     sl₁(A);  ✓ 获得            |
     R₁(A);                     |
     sl₁(B);  ✓ 获得            |
     R₁(B);                     |
     |                          sl₂(B);  ✓ 获得
     |                          R₂(B);
     |                          sl₂(A);  ✓ 获得
     |                          R₂(A);
     尝试 xl₁(B) ...             |
     ✗ 被阻塞！                 尝试 xl₂(A) ...
     (T₂ 持有 B 的 S 锁)        ✗ 被阻塞！
                                (T₁ 持有 A 的 S 锁)

     ⚠ 死锁！T₁ 等 T₂ 释放 B 的 S 锁，T₂ 等 T₁ 释放 A 的 S 锁
```

**死锁形成过程详解**：

1. T₁ 先后获取 A 和 B 的共享锁，完成 R₁(A) 和 R₁(B)
2. T₂ 获取 B 的共享锁（S 锁之间兼容，所以成功），完成 R₂(B)；再获取 A 的共享锁，完成 R₂(A)
3. **T₁ 想升级 B 的锁为排他锁，但 T₂ 持有 B 的 S 锁 → T₁ 等待 T₂ 释放**
4. **T₂ 想升级 A 的锁为排他锁，但 T₁ 持有 A 的 S 锁 → T₂ 等待 T₁ 释放**
5. 形成**循环等待**：T₁ 等 T₂，T₂ 等 T₁ → **死锁**

**死锁的四个必要条件**（全满足）：
- ✓ 互斥条件：排他锁不能被共享
- ✓ 持有并等待：各事务持有锁的同时等待新锁
- ✓ 不可剥夺：锁不能被强制释放
- ✓ 循环等待：T₁ → T₂ → T₁ 形成环路

**等待图（Wait-for Graph）**：
```
T₁ ──等待 B 的 X 锁──→ T₂
T₂ ──等待 A 的 X 锁──→ T₁
    （形成环路 → 死锁）
```

### 举一反三

**新例题**：事务 T₁: R₁(A), W₁(A)；T₂: R₂(B), W₂(B)。它们遵循 2PL 时是否可能死锁？

**答案**：不可能。因为 T₁ 只访问 A，T₂ 只访问 B，两个事务访问的数据项完全不重叠，不存在锁竞争。即使严格按 2PL 加锁（T₁: sl₁(A)→xl₁(A)；T₂: sl₂(B)→xl₂(B)），各自的锁互不冲突。

**核心规律**：死锁的本质是**多个事务交叉等待对方持有的资源**。如果事务访问的数据集不重叠，或者所有事务按相同的全局顺序获取锁，就不会死锁。

---

*以上为第五章问答题（共 30 分）的完整解答。*
