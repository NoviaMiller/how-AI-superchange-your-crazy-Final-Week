# TJU的UU不要拿往年题问老师！问了题型会改！就没有往年题了！不要问不要问！

# 二、SQL 语句答案与解析

## 11. “学生 - 选课”数据库

已知 4 张表：

- `student(sno, sname, sgender, sbirth, sdept)`
- `teacher(tno, tname, tdept)`
- `course(cno, cname, ccredit, tno)`
- `sc(sno, cno, score)`

其中：

- `student` 保存学生信息；
- `course` 保存课程信息；
- `sc` 是学生选课成绩表，用 `sno` 连接学生，用 `cno` 连接课程；
- `teacher` 保存教师信息，本题 3 个 SQL 暂时不需要用到。

## 11.1 列出最高成绩大于 85 的课程编号、名称及最高成绩，并按课程编号降序排序

**答案：**

```sql
SELECT
    c.cno,
    c.cname,
    MAX(sc.score) AS max_score
FROM course AS c
INNER JOIN sc AS sc
    ON c.cno = sc.cno
GROUP BY c.cno, c.cname
HAVING MAX(sc.score) > 85
ORDER BY c.cno DESC;
```

**解析：**

这道题的核心是“每门课程的最高成绩”。成绩在 `sc` 表中，课程名称在 `course` 表中，所以要把两张表连接起来。

`INNER JOIN sc ON c.cno = sc.cno` 的意思是：把课程表和成绩表中课程编号相同的记录配对。这样每条成绩记录就能对应到课程名称。

`MAX(sc.score)` 是聚集函数，用来求一组成绩中的最大值。因为题目要求“每门课程”的最高成绩，所以要按课程分组：

```sql
GROUP BY c.cno, c.cname
```

分组之后，如果要筛选“最高成绩大于 85”的组，不能用 `WHERE MAX(...) > 85`，而要用 `HAVING MAX(...) > 85`。可以把 `WHERE` 理解成“分组前筛选行”，`HAVING` 理解成“分组后筛选组”。

最后，题目要求按课程编号降序排序，所以使用：

```sql
ORDER BY c.cno DESC
```

**举一反三：**

题目：查询每个系的学生人数，只列出人数大于 10 的系，并按人数降序排列。

答案：

```sql
SELECT
    sdept,
    COUNT(*) AS student_count
FROM student
GROUP BY sdept
HAVING COUNT(*) > 10
ORDER BY student_count DESC;
```

简析：按 `sdept` 分组，用 `COUNT(*)` 统计人数，用 `HAVING` 筛选分组结果。

## 11.2 查询选修了“高等数学”的学生的学号、姓名和系别，要求使用 INNER JOIN ... ON

**答案：**

```sql
SELECT
    s.sno,
    s.sname,
    s.sdept
FROM student AS s
INNER JOIN sc AS sc
    ON s.sno = sc.sno
INNER JOIN course AS c
    ON sc.cno = c.cno
WHERE c.cname = '高等数学';
```

**解析：**

题目要查学生的学号、姓名、系别，这些信息在 `student` 表中；但判断学生是否选修了“高等数学”，需要经过 `sc` 表和 `course` 表。

连接路径是：

```text
student.sno -> sc.sno
sc.cno -> course.cno
```

也就是说，先通过学号把学生和选课记录连接起来，再通过课程编号把选课记录和课程连接起来。最后用：

```sql
WHERE c.cname = '高等数学'
```

筛选课程名称为“高等数学”的记录。

注意：题目明确要求使用 `INNER JOIN ... ON`，所以不能只写旧式连接：

```sql
FROM student, sc, course
WHERE student.sno = sc.sno
  AND sc.cno = course.cno
```

虽然旧式写法也能表达连接，但不符合本题要求。

**举一反三：**

题目：查询选修了“数据库原理”的学生姓名和成绩。

答案：

```sql
SELECT
    s.sname,
    sc.score
FROM student AS s
INNER JOIN sc AS sc
    ON s.sno = sc.sno
INNER JOIN course AS c
    ON sc.cno = c.cno
WHERE c.cname = '数据库原理';
```

简析：学生姓名来自 `student`，成绩来自 `sc`，课程名称来自 `course`，所以需要三表连接。

## 11.3 查询选修了全部课程的学生的学号和姓名

**答案一：使用 NOT EXISTS**

```sql
SELECT
    s.sno,
    s.sname
FROM student AS s
WHERE NOT EXISTS (
    SELECT 1
    FROM course AS c
    WHERE NOT EXISTS (
        SELECT 1
        FROM sc AS sc
        WHERE sc.sno = s.sno
          AND sc.cno = c.cno
    )
);
```

**答案二：使用 GROUP BY 和 HAVING**

```sql
SELECT
    s.sno,
    s.sname
FROM student AS s
INNER JOIN sc AS sc
    ON s.sno = sc.sno
GROUP BY s.sno, s.sname
HAVING COUNT(DISTINCT sc.cno) = (
    SELECT COUNT(*)
    FROM course
);
```

**解析：**

“选修了全部课程”是 SQL 中常见的“除法查询”问题。它的含义不是选了很多课，而是：对于课程表 `course` 中的每一门课，这个学生在 `sc` 表中都存在对应的选课记录。

### 方法一：NOT EXISTS

`NOT EXISTS` 写法的思路是：

```text
找这样的学生：
不存在一门课程，使得这个学生没有选这门课。
```

双重否定展开后就是：

```text
没有缺课的学生 = 选修了全部课程的学生
```

内层：

```sql
SELECT 1
FROM sc AS sc
WHERE sc.sno = s.sno
  AND sc.cno = c.cno
```

表示“学生 s 是否选了课程 c”。如果不存在这样的记录，说明该学生缺了这门课。外层再用 `NOT EXISTS` 排除所有“存在缺课”的学生。

### 方法二：GROUP BY + HAVING

这种写法更直观：

1. 按学生分组；
2. 统计每个学生选了多少门不同课程；
3. 如果这个数量等于课程表中的课程总数，就说明该学生选修了全部课程。

这里使用：

```sql
COUNT(DISTINCT sc.cno)
```

是为了避免同一学生同一课程出现重复记录时影响统计结果。

两种写法中，`NOT EXISTS` 更贴近“全部”这类逻辑题的标准表达；`GROUP BY + HAVING` 更容易理解，在考试中也常用。

**举一反三：**

题目：查询借阅了图书馆中全部图书的读者编号。

假设有：

- `reader(rid, rname)`
- `book(bid, bname)`
- `borrow(rid, bid)`

答案：

```sql
SELECT
    r.rid
FROM reader AS r
WHERE NOT EXISTS (
    SELECT 1
    FROM book AS b
    WHERE NOT EXISTS (
        SELECT 1
        FROM borrow AS br
        WHERE br.rid = r.rid
          AND br.bid = b.bid
    )
);
```

简析：不存在一本书是该读者没借过的，就表示该读者借阅了全部图书。
