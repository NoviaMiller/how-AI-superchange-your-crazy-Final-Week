# TJU的UU不要拿往年题问老师！问了题型会改！就没有往年题了！不要问不要问！

# 三、关系代数（8 分）

## 题目 12

假设某数据库中有如下关系：

- 电影关系：**Movies** (title, year)
- 影星关系：**MovieStar** (name, address)
- 出演关系：**StarsIn** (movieTitle, movieYear, starName)

---

## 前置知识：关系代数符号速查表

在正式做题之前，先搞懂关系代数中每个符号是什么意思。你可以把关系代数想象成"用数学符号写 SQL"。

| 符号 | 名称 | 含义（大白话） | SQL 等价写法 |
|:---:|:---|:---|:---|
| σ | **选择 (Select)** | 按条件**筛选行**（水平切） | `SELECT * FROM 表 WHERE 条件` |
| π | **投影 (Project)** | 只保留**指定的列**（垂直切） | `SELECT 列1, 列2 FROM 表` |
| × | **笛卡尔积 (Cartesian Product)** | 两张表的**所有行两两配对** | `SELECT * FROM 表1, 表2`（不带 WHERE） |
| ⋈ | **自然连接 (Natural Join)** | 同名字段值相等的行**自动匹配** | `NATURAL JOIN` |
| ⋈<sub>条件</sub> | **θ 连接 (Theta Join)** | 按**自定义条件**连接两表 | `JOIN ... ON 条件` |
| ∪ | **并 (Union)** | 两个查询结果**合并去重** | `UNION` |
| ∩ | **交 (Intersection)** | 取两个查询结果的**公共部分** | `INTERSECT` |
| − | **差 (Difference)** | 从表1中**减掉**表2也有的行 | `EXCEPT` 或 `NOT IN` |
| ρ | **重命名 (Rename)** | 给表或列**起个别名** | `AS`（别名） |
| ← | **赋值 (Assignment)** | 把中间结果**存到临时变量** | `WITH 临时表 AS (...)` |

> **记忆口诀**：σ 像筛子（筛行），π 像切刀（切列），× 像配对游戏，⋈ 像自动匹配。

---

### 小问（1）：约束条件 —— 属性 name 是影星关系的主关键词（4 分）

#### 什么是"主关键词"（主键 / Primary Key）？

主键就是一张表中**唯一确定一行**的那个字段。比如你的学号，全校唯一，一看学号就能锁定你这个人。主键有两条铁律：

- **不能重复**：两张不同的行不能有相同的主键值；
- **不能为空**：每一行的主键值必须有值（不能是 NULL）。

#### 答案

> **用函数依赖（FD）表示：**
>
> **name → address**
>
> 读作：name 函数决定 address。意思是：只要 name 确定了，address 也唯一确定。

> **用关系代数约束条件表示：**
>
> **|π<sub>name</sub>(MovieStar)| = |MovieStar|**
>
> 解释：投影 name 列后的元组数量 = 原表的元组数量。说明 name 列没有重复值，每个 name 只对应唯一的一行。

> **也可以写成等价条件：**
>
> **σ<sub>name = name' ∧ address ≠ address'</sub>(MovieStar × ρ<sub>name', address'</sub>(MovieStar)) = ∅**
>
> 解释：如果在 MovieStar 和自己的笛卡尔积中，能找到两个元组 name 相同但 address 不同，那就违反了主键约束。约束要求这种情况**不存在**（结果为空集）。

#### 详细解析

**Step 1 — 理解主键约束的本质**

MovieStar 表长这样：

| name | address |
|------|---------|
| 成龙 | 香港 |
| 刘德华 | 香港 |
| 成龙 | 北京 | ← **如果存在这行就违规！因为 name="成龙" 出现了两次**

主键约束就是禁止上面这种情况发生：一个 name 值只能对应一行。

**Step 2 — 用关系代数怎么表达"不能重复"？**

思路：name 列去重后的行数 = 原表总行数。

- π<sub>name</sub>(MovieStar) 的意思是"只看 name 这一列"，如果有重复的 name，去重后行数就会变少。
- |...| 表示数一数有多少行（元组）。
- 如果 |π<sub>name</sub>(MovieStar)| = |MovieStar|，说明没有重复，即 name 唯一。

**Step 3 — 函数依赖（FD）说明**

name → address，意思是 name 能"决定" address。给定一个 name，你就能唯一确定对应的 address。这正是主键的含义。

---

### 小问（2）：查询出演了两部及以上电影的影星的姓名（4 分）

#### 答案

> 设：S1 = StarsIn，S2 = ρ<sub>mt, my, sn</sub>(StarsIn)
>
> （意思是把 S2 中三列分别重命名为 mt, my, sn，避免和 S1 的列名冲突）
>
> **π<sub>starName</sub>(σ<sub>starName = sn ∧ (movieTitle ≠ mt ∨ movieYear ≠ my)</sub>(S1 × S2))**

#### 每一步对应 SQL 的解释

为了让小白也能看懂，这里把关系代数表达式拆成 SQL 来对比：

| 步骤 | 关系代数 | 对应 SQL | 大白话 |
|:---:|:---|:---|:---|
| ① | S1 = StarsIn | `SELECT * FROM StarsIn` | 先拿出完整的出演表 |
| ② | S2 = ρ(StarsIn) | `SELECT * FROM StarsIn` | 再复制一份出演表（改列名） |
| ③ | S1 × S2 | `FROM S1, S2`（笛卡尔积） | 两表的每一行都两两配对 |
| ④ | σ(条件) | `WHERE S1.starName = S2.sn AND (S1.movieTitle ≠ S2.mt OR S1.movieYear ≠ S2.my)` | 筛选：同一个影星，但是**不同电影** |
| ⑤ | π<sub>starName</sub>(...) | `SELECT DISTINCT S1.starName` | 最后只要影星姓名（去重） |

#### 为什么要用笛卡尔积 + 选择？

因为关系代数没有 SQL 的 `GROUP BY ... HAVING COUNT(*) >= 2` 这种聚合功能。那怎么判断一个影星演了几部电影呢？

**巧妙思路**：把 StarsIn 表和自己做笛卡尔积。对于每个影星：

- 如果她只演了 **1 部**电影 → 笛卡尔积中，她只能自己配对自己，而 movieTitle ≠ mt 这个条件会把"自己配自己"排除掉 → **不会出现在结果中**。
- 如果她演了 **2 部或更多**电影 → 笛卡尔积中，A 电影的记录可以和 B 电影的记录配对（同一个人，不同电影）→ **会出现在结果中**。

#### 完整 SQL 等价写法

```sql
-- 用关系代数的思路翻译成 SQL：
SELECT DISTINCT S1.starName
FROM StarsIn S1, StarsIn S2
WHERE S1.starName = S2.starName           -- 同一个影星
  AND (S1.movieTitle <> S2.movieTitle      -- 但电影不同
       OR S1.movieYear <> S2.movieYear);

-- 当然，实际开发中更常用的写法是 GROUP BY + HAVING：
SELECT starName
FROM StarsIn
GROUP BY starName
HAVING COUNT(*) >= 2;
```

---

## 举一反三：总结做题思路

### 关系代数解题通用套路

1. **先翻译需求为 SQL**（因为 SQL 更直观）
2. **再逐步转换为关系代数符号**（σ 代替 WHERE，π 代替 SELECT，× 或 ⋈ 代替 FROM...JOIN）
3. 如果需要"分组统计"（如 COUNT、SUM），关系代数没有直接对应，通常**用笛卡尔积 + 条件筛选**来间接实现。

---

### 新例题 1

> 查询出演过电影 "Hero" 的所有影星的姓名。

**答案**：
**π<sub>starName</sub>(σ<sub>movieTitle = 'Hero'</sub>(StarsIn))**

**SQL 对比**：
```sql
SELECT starName FROM StarsIn WHERE movieTitle = 'Hero';
```

**解析**：先 σ 筛出行（movieTitle = 'Hero' 的那些出演记录），再 π 切出 starName 列。

---

### 新例题 2

> 查询所有影星的姓名和地址（要求：只包含那些至少出演过一部电影的影星）。

**答案**：
**π<sub>name, address</sub>(MovieStar ⋈<sub>name = starName</sub> StarsIn)**

或写成：
**π<sub>name, address</sub>(σ<sub>name = starName</sub>(MovieStar × StarsIn))**

**SQL 对比**：
```sql
SELECT DISTINCT MS.name, MS.address
FROM MovieStar MS
JOIN StarsIn SI ON MS.name = SI.starName;
```

**解析**：先把 MovieStar 和 StarsIn 做连接（按 name = starName 匹配），然后投影需要的列。⋈<sub>条件</sub> 就是带条件的连接，连接条件写在 ⋈ 的右下角。

---

### 新例题 3

> 查询从未出演过任何电影的影星姓名。

**答案**：
**π<sub>name</sub>(MovieStar) − π<sub>starName</sub>(StarsIn)**

**SQL 对比**：
```sql
SELECT name FROM MovieStar
EXCEPT
SELECT starName FROM StarsIn;

-- 或
SELECT name FROM MovieStar
WHERE name NOT IN (SELECT starName FROM StarsIn);
```

**解析**：先从所有影星中取出 name，再减去出演表中出现过的 starName，剩下的就是"没演过电影"的影星。"减去"在关系代数中用 **−**（差运算），前提是两个集合的列数、列类型一致（这里都是一列字符串）。
